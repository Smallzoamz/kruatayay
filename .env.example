# SiteRestaurant Environment Variables
# Copy this file to .env (or set in Vercel Environment Variables)

# Server Port (optional, defaults to 3000)
PORT=3001

# =====================================================
# Menu Sync Settings (Required for POS Integration)
# =====================================================
# Secret key for webhook authentication (must match WEBSITE_SYNC_SECRET in POS)
SYNC_SECRET=pos-website-sync-2026
